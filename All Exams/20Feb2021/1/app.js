
function solve() {
   // 1. Get needed elements 
   // Make form - prevent defaulst
   // Make event listener for button and move created element
   // Clear form inputs

   let postsElement = document.querySelector('.site-content main section')
   let inputCreatorElement = document.getElementById('creator')
   let inputTitleElement = document.getElementById('title')
   let inputCategoryElement = document.getElementById('category')
   let inputContentElement = document.getElementById('content')
   let createBtnElements = document.querySelector('.btn.create')
}